import React from 'react';
import PropTypes from 'prop-types';
import './card.css';

const card = (props) => {
    return (
        <div className='card-container'>
            
        </div>
    );
};

card.propTypes = {
    
};

export default card;